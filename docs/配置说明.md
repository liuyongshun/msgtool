# MsgSkill 配置说明文档

**配置文件**: `config/sources.json`  
**最后更新**: 2026-02-10
**新增功能**: 
- 翻译配置开关（所有数据源独立控制）
- LLM 时间过滤（仅处理最近 N 天内的数据）
- Notion 自动同步开关（各数据源独立控制）

---

## 📋 配置文件结构

```json
{
  "sources": {
    "news": { ... },      // 新闻源配置
    "rss": { ... },       // RSS 源配置
    "github": { ... },    // GitHub 源配置
    "arxiv": { ... }      // arXiv 论文配置
  },
  "global_settings": {
    "maintenance": { ... },   // 维护配置
    "scheduler": { ... }      // 调度器配置
  },
  "llm": { ... }         // AI 模型配置
}
```

---

## 🔧 全局配置 (global_settings)

### 基础设置

```json
{
  "default_cache_ttl": 300,           // 默认缓存时间（秒）
  "default_fetch_limit": 10,          // 默认抓取数量
  "max_fetch_limit": 50,              // 最大抓取数量
  "request_timeout": 30,              // 请求超时时间（秒）
  "user_agent": "Mozilla/5.0 ..."    // HTTP 请求 User-Agent
}
```

### 维护配置 (maintenance)

```json
{
  "maintenance": {
    "log_retention_days": 7,          // 日志保留天数
    "cache_retention_days": 30,       // 缓存保留天数
    "auto_cleanup": {
      "enabled": true,                // 启用自动清理
      "log_cleanup_time": "03:00",    // 日志清理时间（每天）
      "cache_cleanup_time": "02:00",  // 缓存清理时间
      "cache_cleanup_day": "sunday"   // 缓存清理日期（每周）
    }
  }
}
```

**说明**:
- `log_retention_days`: 保留最近N天的日志文件
- `cache_retention_days`: 保留最近N天的缓存文件
- `auto_cleanup.enabled`: 启用后，调度器会自动执行清理任务

### 调度器配置 (scheduler)

```json
{
  "scheduler": {
    "enabled": true,                  // 全局开关
    "tasks": {
      "arxiv": {
        "enabled": true,              // arXiv 任务开关
        "time": "09:00",              // 执行时间
        "max_results": 20,            // 每个分类最多抓取论文数
        "translation_strategy": {
          "enabled": true,
          "selective_translation": true,  // 选择性翻译
          "min_authors": 2                // 最小作者数阈值
        }
      },
      "hackernews": {
        "enabled": true,
        "time": "10:00",
        "max_results": 50
      },
      "rss": {
        "enabled": true,
        "time": ["06:00", "08:00", ..., "22:00"],  // 多个时间点
        "max_results": 20
      },
      "github": {
        "enabled": true,
        "time": "14:00",
        "max_results": 100
      }
    }
  }
}
```

**说明**:
- `enabled`: 全局开关，关闭后所有任务都不执行
- `tasks.*.enabled`: 单个任务开关
- `tasks.*.time`: 执行时间（24小时制），支持字符串或数组
- `tasks.*.max_results`: 控制抓取数量

---

## 🤖 AI 模型配置 (llm)

```json
{
  "llm": {
    "enabled": true,                            // 启用 AI 功能
    "provider": "deepseek",                     // 提供商
    "api_key": "sk-xxxxxxxxxx",                 // API 密钥
    "api_url": "https://api.deepseek.com/v1/chat/completions",
    "model_name": "deepseek-chat",              // 模型名称
    "max_tokens": 1000,                         // 最大 token 数
    "temperature": 0.3,                         // 温度参数
    "recent_days": 7                            // 时间过滤：仅处理最近 N 天内的数据（新增）
  }
}
```

**参数说明**:
- `enabled`: 启用后才会进行 AI 筛选和翻译
- `api_key`: DeepSeek API 密钥（获取地址：https://platform.deepseek.com/）
- `max_tokens`: 控制 AI 响应长度
- `temperature`: 控制 AI 输出随机性（0.0-1.0，越低越确定）
- `recent_days`: **时间过滤参数**（新增，默认 7 天）
  - 仅对 HackerNews、RSS、arXiv 生效
  - 在调用 LLM（AI筛选/翻译）前，先过滤掉超过 N 天的旧数据
  - 可有效减少不必要的 Token 消耗
  - 建议值：7-14 天（根据内容更新频率调整）

**Token 优化建议**:
- arXiv 选择性翻译可节省 76% token
- GitHub 白名单缓存可节省 70-85% token
- **时间过滤**（`recent_days`）可节省 30-50% token（取决于数据源更新频率）

---

## 📝 Notion 同步配置 (notion_sync)

```json
{
  "notion_sync": {
    "enabled": true,                            // 全局开关
    "api_token": "ntn_xxxxxxxxxx",              // Notion Integration Token
    "auto_sync": {                              // 自动同步开关（各数据源独立控制）
      "github": false,                          // GitHub 自动同步
      "rss": false,                             // RSS 自动同步
      "hackernews": false,                      // HackerNews 自动同步
      "arxiv": false                            // arXiv 自动同步
    },
    "databases": {
      "github": {
        "database_id": "303e0cb0...",           // GitHub 数据库 ID
        "description": "GitHub项目数据库"
      },
      "rss": {
        "database_id": "303e0cb0...",           // RSS 数据库 ID（可选）
        "description": "RSS文章数据库"
      },
      "hackernews": {
        "database_id": "303e0cb0...",           // HackerNews 数据库 ID（可选）
        "description": "HackerNews新闻数据库"
      },
      "arxiv": {
        "database_id": "",                      // arXiv 数据库 ID（可选，为空则使用github数据库）
        "description": "arXiv论文数据库"
      }
    }
  }
}
```

**参数说明**:
- `enabled`: 全局开关，关闭后所有 Notion 同步功能失效
- `api_token`: Notion Integration Token（获取方式见 [Notion同步使用指南](./Notion同步使用指南.md)）
- `auto_sync.*`: **自动同步开关**（新增）
  - 控制每个数据源在抓取完成后是否自动同步到 Notion
  - `true`: 抓取完成后自动同步
  - `false`: 不自动同步（可通过预览页手动同步单条）
- `databases.*.database_id`: 各数据源对应的 Notion 数据库 ID
  - 如果某个数据源的 `database_id` 为空，会使用 `github` 数据库作为默认数据库

**使用场景**:
- **自动同步**（`auto_sync.* = true`）: 适合需要实时同步所有抓取内容的场景
- **手动同步**（`auto_sync.* = false`）: 适合需要筛选后再同步的场景，可通过预览页的「同步到 Notion」按钮手动选择

**详细说明**: 参见 [Notion同步使用指南](./Notion同步使用指南.md)

---

## 📰 数据源配置

### 1. HackerNews 配置

**配置位置**: `sources.news.hackernews`

```json
{
  "enabled": true,
  "name": "Hacker News",
  "type": "api",
  "api_base_url": "https://hacker-news.firebaseio.com/v0",
  "story_types": ["top", "new", "best"],       // 新闻类型
  "ai_filter_enabled": true,                   // 启用 AI 筛选
  "translation_enabled": true,                 // 启用文章翻译（新增）
  "fetch_limit": {
    "default": 10,
    "max": 50
  }
}
```

**配置项说明**:
- `translation_enabled`: 控制是否启用文章翻译（默认 `true`，设为 `false` 可节省 Token）
- `ai_filter_enabled`: 启用 AI 筛选，仅保留 AI 相关文章

**story_types 说明**:
- `top`: 热门新闻
- `new`: 最新新闻
- `best`: 高分新闻

### 2. RSS 源配置

**配置位置**: `sources.rss.*`

**单个 RSS 源示例**:
```json
{
  "mit_tech_review": {
    "enabled": true,                          // 是否启用
    "name": "MIT Technology Review AI",       // 显示名称
    "url": "https://www.technologyreview.com/feed/",  // RSS URL
    "description": "MIT科技评论AI频道",        // 描述
    "category": "technology_news",            // 分类
    "tags": ["AI资讯", "技术博客"],            // 标签
    "ai_filter_enabled": true,                // 是否启用 AI 筛选
    "translation_enabled": true               // 启用文章翻译（新增）
  }
}
```

**配置项说明**:
- `translation_enabled`: 控制是否启用文章翻译（默认 `true`）
- `ai_filter_enabled`: 启用 AI 筛选，仅保留 AI 相关文章

**category 类别**:
- `technology_news`: 科技新闻
- `company_blog`: 公司博客
- `personal_blog`: 个人博客
- `newsletter`: 邮件通讯
- `tech_community`: 技术社区
- `ai_tools`: AI 工具
- `papers`: 论文
- `trends`: 趋势分析
- `engineering`: 工程实践
- `news`: 新闻媒体

**RSS 源状态说明**:
- 当前配置中部分 RSS 源可能因网络、URL 变更等原因出现 404/连接失败
- 建议定期检查 `output/daily/YYYY-MM-DD/rss_*.json` 中的 `errors` 字段
- 对于长期失败的源，建议在配置中设置 `enabled: false` 以节省抓取时间
- 部分源使用 `localhost:8878`（RSSHub 本地实例），需确保 RSSHub 服务运行

**tags 标签**:
- `AI资讯`: 通用 AI 新闻
- `AI工具`: AI 工具和产品
- `AI论文`: 学术论文
- `技术博客`: 技术深度内容
- `趋势分析`: 行业趋势

### 3. GitHub 配置

**配置位置**: `sources.github.trending_daily`

```json
{
  "enabled": true,
  "languages": ["python", "javascript", "typescript", "rust"],
  "topics": ["ai"],                            // 搜索关键词
  "trending_types": ["pushed", "created", "stars"],
  "star_limits": {
    "pushed": 500,                             // pushed 类型最小 star 数
    "created": 100,                            // created 类型最小 star 数
    "stars": 500                               // stars 类型最小 star 数
  },
  "ai_filter_enabled": true,                   // 启用 AI 筛选
  "translation_enabled": true,                 // 启用项目翻译（新增）
  "database_enabled": true                     // 启用 GitHub 数据库功能（新增）
}
```

**配置项说明**:
- `translation_enabled`: 控制是否启用项目描述翻译（默认 `true`）
- `ai_filter_enabled`: 启用 AI 筛选，仅保留 AI 相关项目
- `database_enabled`: 启用 GitHub 数据库功能（默认 `true`），实现智能去重和持久化存储

**trending_types 说明**:
- `pushed`: 最近7天有推送的项目
- `created`: 最近180天创建的项目
- `stars`: 最近180天创建的高 star 项目

**语言支持**:
- `python`
- `javascript`
- `typescript`
- `rust`

### 4. arXiv 论文配置

**配置位置**: `sources.arxiv.*`

**单个分类示例**:
```json
{
  "cs_ai": {
    "enabled": true,                          // 是否启用
    "name": "Artificial Intelligence",        // 显示名称
    "category": "cs.AI",                      // arXiv 分类代码
    "description": "人工智能领域论文",         // 描述
    "tags": ["AI论文"],                       // 标签
    "translation_enabled": true               // 启用论文翻译（新增）
  }
}
```

**配置项说明**:
- `translation_enabled`: 控制是否启用论文摘要翻译（默认 `true`）

**已支持的分类**（共 13 个）:
- `cs.AI` - 人工智能
- `cs.LG` - 机器学习
- `cs.CL` - 自然语言处理（NLP）
- `cs.CV` - 计算机视觉与模式识别
- `cs.NE` - 神经网络与进化计算
- `cs.RO` - 机器人
- `stat.ML` - 统计机器学习
- `cs.IR` - 信息检索（RAG、向量检索、检索增强生成）
- `cs.SE` - 软件工程（AI辅助编程、代码大模型）
- `cs.MA` - 多智能体系统（Multi-Agent、协作AI）
- `cs.HC` - 人机交互（AI Agent界面、交互设计）
- `cs.DC` - 分布式计算（大模型训练、推理优化）
- `eess.AS` - 音频与语音处理（语音大模型）

---

## 🎯 配置示例与扩容

### 添加新的 RSS 源

在 `sources.rss` 中添加：

```json
{
  "new_source": {
    "enabled": true,
    "name": "新数据源",
    "url": "https://example.com/feed.xml",
    "description": "描述",
    "category": "news",
    "tags": ["AI资讯"],
    "ai_filter_enabled": true,
    "translation_enabled": true
  }
}
```

### 添加新的 arXiv 分类

在 `sources.arxiv` 中添加：

```json
{
  "cs_sy": {
    "enabled": true,
    "name": "Systems and Control",
    "category": "cs.SY",
    "description": "系统与控制领域论文",
    "tags": ["AI论文"],
    "translation_enabled": true
  }
}
```

### 调整调度器配置

```json
{
  "scheduler": {
    "tasks": {
      "arxiv": {
        "enabled": true,
        "time": "09:00",
        "max_results": 20
      },
      "github": {
        "enabled": true,
        "time": "21:52",
        "max_results": 100
      }
    }
  }
}
```

**说明**: 修改配置后需重启调度器生效

---

## 🔍 配置验证

```bash
# 验证 JSON 格式
python3 -c "import json; json.load(open('config/sources.json'))"
```

---

## ⚙️ Token 优化配置

### 1. 时间过滤（`llm.recent_days`）

仅处理最近 N 天内的数据，节省 30-50% Token：

```json
{
  "llm": {
    "recent_days": 7    // 仅处理最近 7 天的数据
  }
}
```

### 2. arXiv 选择性翻译

只翻译多作者论文，节省 76% Token：

```json
{
  "scheduler": {
    "tasks": {
      "arxiv": {
        "translation_strategy": {
          "selective_translation": true,
          "min_authors": 2
        }
      }
    }
  }
}
```

### 3. GitHub 白名单缓存

自动启用，无需配置。已筛选的 AI 项目缓存 30 天，节省 70-85% Token。

### 4. 翻译开关优化

根据内容类型禁用不必要的翻译：

```json
{
  "translation_enabled": false    // 英文技术博客可禁用
}
```

**建议**:
- ✅ 始终启用：arXiv 论文、GitHub 项目
- ⚠️ 可选启用：英文技术博客
- ❌ 建议禁用：中文内容

### 5. max_results 参数说明

| 数据源 | 含义 | AI筛选 | 输出策略 |
|--------|------|--------|---------|
| arXiv | 每个分类最多抓取论文数 | ❌ | 全部保存 |
| HackerNews | 原始数据抓取上限 | ✅ | AI筛选后全部保存 |
| RSS | 每个源最多抓取条目数 | ✅ | AI筛选后全部保存 |
| GitHub | 每种查询类型抓取上限 | ✅ | AI筛选后全部保存 |

**重要**: `max_results` 只控制原始数据量，AI 筛选后的所有结果都会保存。

---

## 🔒 安全配置

### API 密钥管理

**⚠️ 重要**:
1. 不要将包含真实 API 密钥的配置文件提交到公开仓库
2. 使用 `.gitignore` 保护配置文件
3. 定期更换 API 密钥

### 访问控制

**预览服务** 默认绑定到 `localhost:5001`，仅本地访问。

如需外网访问，需修改 `src/msgskill/preview_server.py`：
```python
app.run(host='0.0.0.0', port=5001)  # 允许外网访问（慎用）
```